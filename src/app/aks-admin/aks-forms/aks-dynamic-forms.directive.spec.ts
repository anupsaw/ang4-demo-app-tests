import { AksDynamicFormsDirective } from './aks-dynamic-forms.directive';

describe('AksDynamicFormsDirective', () => {
  it('should create an instance', () => {
    const directive = new AksDynamicFormsDirective();
    expect(directive).toBeTruthy();
  });
});
